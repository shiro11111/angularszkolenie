import { ValidTrueDirective } from './valid-true.directive';

describe('ValidTrueDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidTrueDirective();
    expect(directive).toBeTruthy();
  });
});
